<%= simple_form_for @book do |f| %>
  <%= f.input :name, label:"Book Name" %>
  <%= f.input :isbn %>
  <%= f.input :genre %>
  <%= f.input :editorial %>
  <%= f.association :authors, hint: "Only added authors will be on this list" %>
  <%= f.button :submit %>
<% end %>