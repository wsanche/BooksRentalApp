<%= simple_form_for @reading_list do |f| %>
	<%= f.input :name, label: "Reading List Name" %>
	<%= f.association :books, hint: "Only added books will be on this list" %>
	<%= f.button :submit %>
<% end %>